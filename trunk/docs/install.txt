INSTALLATION
===============



If you use the EGG system, you just need to run "easy_install deblib" or
"easy_install -U deblib" if it's already installed.
Otherwise, you must have the python-dev package installed and run "python
setup.py install"
